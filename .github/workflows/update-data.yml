name: Update Regional Map Data

on:
  schedule:
    - cron: '0 3 * * 1'
  workflow_dispatch:

# Prevents multiple workflow runs from executing at the exact same time
concurrency:
  group: update-map-data
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  update-map:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run Fetch Script
        run: node fetch-region.js

      - name: Commit and Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add region-map-data.json
          
          # Only attempt to commit and push if the JSON file actually changed
          if ! git diff-index --quiet HEAD; then
            git commit -m "chore: auto-update map data [skip ci]"
            
            # Fetch the absolute latest remote state right before pushing
            git fetch origin main
            # Rebase our new commit cleanly on top of remote
            git pull origin main --rebase --autostash
            # Push the changes
            git push origin main
          else
            echo "No changes to map data detected. Skipping commit."
          fi
